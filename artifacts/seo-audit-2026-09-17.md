# SEO audit — 2026-09-17

## Scope and evidence

- Live URL audited: `https://www.revelationagency.com/`
- Official source drawer: `Revelation-Agency/revelationagency.com`, current `main` commit `338daf09dbd67ba53d687ec234b8c82c77eb2295`.
- Canon ledger 47 authorizes reversible own-site SEO for Clovis and Fresno.
- No production, DNS, Search Console, or paid-data setting was changed in this audit.

## Verified baseline

- The root, apex host, and `www` host consolidate to the canonical `https://www.revelationagency.com/` address.
- Homepage title and description are present and concise.
- `robots.txt` and `sitemap.xml` each returned HTTP 200. The sitemap contains 144 unique canonical `www` URLs, including 28 location pages and 11 editorial pages.
- The existing offline site verifier passed all 19 checks before this change.

## Implemented improvement

The homepage structured data now uses a connected graph:

1. The existing `ProfessionalService` entity continues to carry the public business identity, contact details, service area, and verified social profiles.
2. Its description now matches the page's public search description, including Clovis and Fresno.
3. A `WebSite` entity identifies the canonical site and references the public business entity as publisher.

This makes the business, its location focus, and its website relationship internally consistent for machines that consume Schema.org. It is not a claim of a guaranteed ranking or rich-result change.

## Tool availability

- GEO Optimizer completed a public-page audit. Its 55/100 vendor score is a directional checklist, not a search-ranking or AI-citation measurement.
- Google Search Console MCP connected but cannot list properties because no approved OAuth or service-account credential is configured. No authentication flow was opened.
- OpenSEO was started locally and queried read-only: its self-hosted `admin@localhost` workspace contains only the default project. Health checks confirm that no DataForSEO provider is configured, so no paid keyword, competitor, or backlink request was made.

## Follow-up items

- Connect an approved Search Console credential to obtain first-party clicks, queries, indexing, and sitemap data.
- Decide separately whether public frontend webhook architecture should be replaced with a server-side form endpoint; that is a security/lead-routing change, not part of this SEO edit.
- Re-run the public audit after any approved production deployment.
## Additional repair

Five location pages referenced the analytics script through an invalid relative path. Each now uses the root-relative /assets/js/analytics-events.js address, which the site test verifies resolves.
